#include<iostream>

int factorial(int n){
    
    if (n<=1){
        return 1;
    }
    return n*factorial(n-1);
}

}

int main() {
    int a;
    std::cout<<"Enter a number"<<"\n";
    std::cin>>a;
     std::cout<<"Factorial of the number is "<< factorial(a)<<'\n';

    return 0;
}
