#include <iostream>

using namespace std;

int length(char s[]){
    if(s[0]=='\0'){
        return 0;
    }
    
    int smallStringOutput= length(s+1);
    return smallStringOutput+1;
}

int main()
{
    char s[100];
    
    cin>>s;
    int l= length(s);
    
    

    return 0;
}
